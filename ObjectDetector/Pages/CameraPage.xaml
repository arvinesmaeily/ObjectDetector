<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ObjectDetector.Pages.CameraPage"
             Title="Camera"
             BackgroundColor="{AppThemeBinding Light=White, Dark=#121212}">

    <Grid RowDefinitions="*,Auto" ColumnDefinitions="*">
        <!-- Main camera and overlay area -->
        <Grid Grid.Row="0" Grid.Column="0" RowDefinitions="*" ColumnDefinitions="*">
            <!-- Camera preview -->
            <toolkit:CameraView
                x:Name="Camera"
                Grid.Row="0"
                Grid.Column="0"/>

            <!-- Overlay for YOLO boxes -->
            <GraphicsView
                x:Name="OverlayView"
                Grid.Row="0"
                Grid.Column="0"
                BackgroundColor="Transparent"
                InputTransparent="True" />

            <!-- Stats panel (top-left) -->
            <VerticalStackLayout
                Grid.Row="0"
                Grid.Column="0"
                Padding="10"
                Spacing="5"
                VerticalOptions="Start"
                HorizontalOptions="Start">
                <Label x:Name="FpsLabel" Text="FPS: 0" FontSize="12" TextColor="White" />
                <Label x:Name="DetectionCountLabel" Text="Detections: 0" FontSize="12" TextColor="White" />
            </VerticalStackLayout>
        </Grid>

        <!-- Controls panel (bottom) -->
        <Grid Grid.Row="1" Grid.Column="0" ColumnDefinitions="*" RowDefinitions="Auto" 
              Padding="10" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">

            <!-- Buttons Row -->
            <HorizontalStackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="Center" Spacing="10">
                <Button 
                    x:Name="CaptureButton"
                    Text="📷 Capture"
                    Clicked="OnCaptureClicked"
                    BackgroundColor="#007ACC"
                    TextColor="White"
                    Padding="15,10"
                    FontSize="14" />
                <Button 
                    x:Name="ToggleDetectionsButton"
                    Text="👁️ Hide Boxes"
                    Clicked="OnToggleDetections"
                    BackgroundColor="#6A737D"
                    TextColor="White"
                    Padding="15,10"
                    FontSize="14" />
            </HorizontalStackLayout>
        </Grid>
    </Grid>

</ContentPage>